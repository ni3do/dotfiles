---
description: Commit current changes with a concise conventional commit
argument-hint: "[scope or message hint]"
---
Commit the current changes. Hint: $ARGUMENTS

Before committing:
- Inspect `git status` and relevant diffs.
- Run lightweight validation if obvious and safe.
- Use the repo's commit conventions.
- Do not include unrelated files.
- Use a concise Conventional Commits-style message.
- If this repo requires pushing after commits, push before handing back.
